stages:
  - build
  - deploy

build-job:
  stage: build
  script:
    - docker build -t intpoln/taskflow .
  only:
    - main

deploy-job:  
  stage: deploy
  script:
    - docker compose down
    - docker compose up -d
  only:
    - main
  needs:
    - build-job